- 画像入力対応(2026-07-02): current-task.mdの指定通り `{type:'image', source:{type:'url', url}}` をClaudeに送ると、実在する公開URL（Wikimedia Commons）に対しても毎回 `400 Unable to download the file` で失敗した。原因はこのbotのOAuth/Maxサブスクリプション認証経路（`anthropic-beta: oauth-2025-04-20`）ではAnthropic側のサーバーサイドURL取得が機能しない模様（生APIキー利用時の一般的なドキュメント例とは挙動が異なる）。対処: `provider.js` の `toClaudeMessages` を非同期化し、画像URLを自前でfetchしてbase64化してから `source:{type:'base64', media_type, data}` として送る方式に変更。実トークンで再検証し、Claudeが画像内容を具体的に描写する応答を安定して返すことを確認済み。
- 単一ループ化の検証(2026-07-02): Fable 5のadaptive thinkingは、自明なツール呼び出し（「#Xを読んで」等、personaの行動原則に直結する依頼）ではthinkingブロックを出力しないことがある（out=51トークンでtool_useのみ、effort=maxでも同様）。thinkingブロック保存の検証時は、複数チャンネルの矛盾情報の突き合わせなど「分析を要するタスク」で誘発すること。実装側は無加工保存なのでモデルが出せば必ず保存される。
- Phase2機能C thinking要約プローブ(2026-07-02): `output_config: {thinking:{display:'summarized'}}` を claude-fable-5 に送ると400「`output_config.thinking: Extra inputs are not permitted`」。2回目にトップレベル`thinking:{type:'enabled',budget_tokens:1024}`を併記（実Anthropic公開APIの拡張思考アクティベーション形）しても同一エラー。このOAuth/Maxサブスクプロキシの`output_config`は`effort`以外のキーを一切受け付けない模様。2回とも400のため仕様どおり実装せず撤退（コードへの組み込みなし）。要約thinking表示を将来やるなら、まず`output_config`以外の経路（別パラメータ名/別beta）でサーバー側要約の口があるかをAnthropic側で確認するところから。
